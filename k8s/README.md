Exemplu minimal de deploy pe Kubernetes (orientativ).  
Nu include Ingress/TLS. Pentru cloud: folosește LoadBalancer/Ingress.

- `00-namespace.yaml`
- `01-config.yaml`
- `10-*.yaml` deployments + services
